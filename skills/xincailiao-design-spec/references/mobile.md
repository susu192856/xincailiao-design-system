# 移动与窄屏规则

- 主规格为桌面端；移动规则用于保证任务可完成，不简单缩小桌面页面。
- 触控目标最小 44px。
- 多列表单回退为单列，标签统一上置。
- 表格使用横向滚动、卡片化摘要或字段优先级，不能压缩全部列。
- 侧栏转抽屉，Tabs 可横向滚动，面包屑折叠中间层。
- Modal/Drawer 在小屏可全屏；底部主操作保持可见。
- Tooltip 不作为触屏唯一信息入口。
- 页面不得产生非组件内部的横向溢出。

